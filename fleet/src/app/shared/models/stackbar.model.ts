export interface Property{
    minVal?: number;
    maxVal?: number;
    activeColor: string;
    bgColor: string;
    bdRadius: string;
    rangeVal: number;
    verleLabel: boolean;
    legend: boolean;
}

export interface DataList{
    value: string;
    title: string;
}